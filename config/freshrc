# keep fresh fresh
fresh freshshell/fresh bin/fresh --bin

fresh config/Xresources --file
fresh config/zprofile --file
fresh config/zshrc --file
fresh config/vimrc --file

fresh config/termite --file=~/.config/termit/config
