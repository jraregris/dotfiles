# keep fresh fresh
fresh freshshell/fresh bin/fresh --bin

fresh config/Xresources --file
fresh config/zprofile --file
fresh config/vimrc --file
fresh config/tmux.conf --file

fresh-options --file=~/.zshrc
  fresh config/zshrc.$(hostname -s) --ignore-missing
  fresh config/zshrc
fresh-options
